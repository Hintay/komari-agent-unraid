**Komari Agent**

Runs the [Komari](https://github.com/komari-monitor) monitoring agent on the unraid host: per-architecture binary cached on the USB flash, auto-start on boot, config persisted across reboots, crash recovery via watchdog, and optional self-update.
